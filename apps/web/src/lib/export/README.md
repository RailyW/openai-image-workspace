# 导出模块

本模块将浏览器 IndexedDB 中的数据导出为 zip。

## 导出内容

- `manifest.json`
- `providers.json`
- `settings.json`
- `presets.json`
- `prompt-snippets.json`
- `tasks.json`
- `images/`

## 隐私提醒

导出包可能包含 Bearer Token、Prompt 和图片结果。应用只负责生成导出文件，不会上传或保存到服务端。
