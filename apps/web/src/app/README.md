# 应用壳

应用壳负责主导航、侧栏品牌区和各功能页切换。

当前实现使用左侧 shadcn 风格导航和本地 React 状态管理当前视图，不引入路由库。内容区不再额外渲染每页顶部标题栏，页面标题由各业务卡片自行表达。所有用户数据由各功能模块写入 IndexedDB。

## 文件说明

- `App.tsx`：定义应用壳、OpenAI 标志、导航项、图标和当前页面渲染逻辑。
- `App.test.tsx`：覆盖主要页面切换，并验证主要表单不再渲染原生 `select`、流式预览开关使用统一的按钮式 checkbox 控件、应用壳不再展示顶部页面 header。
